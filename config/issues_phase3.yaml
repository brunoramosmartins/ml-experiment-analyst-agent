[AGENT] Integrate custom tools into deepagents agent|Phase 3: Agent Assembly|feature,agent,phase-3|## Context\nWith the 5 tools ready (Phase 2), this issue connects them to the deepagents agent. deepagents receives tools via the tools parameter of create_deep_agent. After this issue, the agent must be able to call any tool in response to a natural language query.\n\n## Tasks\n- [ ] Import all 5 tools in src/agent/builder.py\n- [ ] Pass tools to create_deep_agent(tools=[...])\n- [ ] Test individual call of each tool via specific prompt\n- [ ] Verify FilesystemBackend is saving files to correct path\n- [ ] Document: which tools are called in which situations\n\n## Definition of done\n- [ ] Agent created without errors with all 5 tools\n- [ ] Each tool can be invoked via natural language prompt\n- [ ] generate_report saves file to data/agent-workspace/reports/\n- [ ] Smoke test passing: invoke with "analyze experiment X"\n\n## References\n- deepagents tools param: https://docs.langchain.com/oss/python/deepagents/overview\n- Phase 2 tools: src/tools/
[AGENT] Engineer and validate system prompt|Phase 3: Agent Assembly|prompt-engineering,agent,phase-3|## Context\nThe system prompt defines the agent's behavior, workflow, and stopping criteria. A well-written prompt reduces hallucinations, improves tool call sequencing, and makes agent behavior predictable. This is an iterative issue — expect multiple prompt versions before converging.\n\n## Tasks\n- [ ] Create src/agent/prompts/system_prompt.md with content defined in Phase 3\n- [ ] Implement prompt loading from file (not inline)\n- [ ] Iteration 1: test with 5 basic queries, observe behavior in LangSmith\n- [ ] Iteration 2: adjust based on observed failures (wrong tool calls, wrong order)\n- [ ] Iteration 3: test edge cases (empty experiment, MLflow offline, ambiguous query)\n- [ ] Document prompt versions in docs/prompt-engineering-log.md\n\n## Definition of done\n- [ ] system_prompt.md with final documented version\n- [ ] docs/prompt-engineering-log.md with at least 3 documented iterations\n- [ ] Agent consistently follows the 6-step workflow defined in the prompt\n- [ ] Edge cases handled with clear messages to the user\n\n## References\n- deepagents system_prompt guidelines: deepagents repository README\n- Prompt engineering best practices: https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview
[AGENT] Validate agent end-to-end with seed experiments|Phase 3: Agent Assembly|testing,agent,phase-3|## Context\nEnd-to-end validation confirms that all pieces work together: agent, tools, MLflow, LangSmith, FilesystemBackend. This issue is the integrated smoke test of the project before adding advanced observability.\n\n## Tasks\n- [ ] Create scripts/run_demo.py with 3 demonstration queries\n- [ ] Query 1: "Analyze the binary-classification experiment and give me a report"\n- [ ] Query 2: "Compare the 3 best runs from the regression-v2 experiment"\n- [ ] Query 3: "Analyze the overfit-test experiment and identify problems"\n- [ ] For each query: verify generated report, LangSmith trace, absence of errors\n- [ ] Capture LangSmith dashboard screenshots for all 3 executions\n- [ ] Document results in docs/e2e-validation.md\n\n## Definition of done\n- [ ] scripts/run_demo.py executing 3 queries without errors\n- [ ] 3 Markdown reports generated in data/agent-workspace/reports/\n- [ ] 3 complete traces visible in LangSmith\n- [ ] Agent detected overfitting in the overfit-test experiment (query 3)\n- [ ] docs/e2e-validation.md with screenshots and results analysis\n\n## References\n- Seed experiments: docs/demo-experiments.md\n- LangSmith dashboard: https://smith.langchain.com/
