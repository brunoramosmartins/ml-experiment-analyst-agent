# Placeholder — remove when real unit tests are added in Phase 1.


def test_placeholder() -> None:
    """Ensures pytest collects at least one test while src modules are not yet implemented."""
    assert True
